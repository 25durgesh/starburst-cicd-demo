- name: Run Query on Starburst Galaxy
  env:
    TRINO_PASSWORD: ${{ secrets.SB_PASSWORD }}
  run: |
    ./trino \
    --server https://aws-us-east-1-free.galaxy.starburst.io:443 \
    --user ${{ secrets.durgeshcse25@gmail.com }} \
    --Durgesh@2026 \
    --catalog tpch \
    --schema sf1 \
    --execute "SELECT 1"
